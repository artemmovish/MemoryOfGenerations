<Window x:Class="TestWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWPF"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Title="EPUB Reader" Height="1920" Width="1080">
    <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Content="Открыть" Click="OpenEpub_Click" Height="900" Margin="5"/>

            <!-- Кнопки навигации -->
            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
                <Button Content="← Назад" Click="PreviousChapter_Click" Margin="0,0,5,0"/>
                <Button Content="Вперёд →" Click="NextChapter_Click"/>
            </StackPanel>

            <!-- WebView2 -->
            <wv2:WebView2 Name="webView" Grid.Row="2" Width="600" Height="600"/>
        </Grid>
    </ScrollViewer>
</Window>
